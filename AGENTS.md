# AGENTS.md

## 1. 프로젝트 목표

이 프로젝트는 채팅 서비스의 **부하테스트 성능 개선 및 안정성 확보**를 목표로 한다.

최적화 과정에서는 단순 기능 개선보다 다음 우선순위를 따른다.

1. 제공된 E2E 테스트 통과
2. 기존 사용자 동작 및 API 계약 유지
3. 부하 상황에서 오류율 최소화
4. 응답시간 및 처리량 개선
5. 인프라 자원 효율화

성능 개선을 위해 내부 구현은 자유롭게 변경할 수 있지만, **제공된 테스트가 기대하는 외부 동작은 변경하지 않는다.**

---

# 2. 테스트 관련 핵심 규칙

## 제공된 E2E 테스트를 기준으로 구현한다

채점은 제공된 E2E 테스트 및 부하테스트 시나리오를 기준으로 진행된다.

* Playwright E2E 테스트: 총 23개

  * 인증: 9개
  * 채팅: 10개
  * 프로필: 4개
* 부하 주입용 Artillery 시나리오: 총 7개

  * 인증: 2개
  * 채팅: 4개
  * 프로필: 1개
* 테스트 환경은 Chromium 기준이다.
* Playwright E2E가 정상적으로 통과해야 부하 주입 단계로 진행할 수 있다.

따라서 기능 또는 성능 개선 후 반드시 E2E 테스트를 실행하여 회귀 여부를 확인한다.

```bash
npm test
```

필요한 경우 특정 테스트나 UI 모드를 사용한다.

```bash
npm test tests/auth.spec.js
npm run test --ui
npm run test --headed
```

---

## 테스트 코드를 임의로 수정하지 않는다

**테스트를 통과시키기 위해 제공된 E2E 테스트 코드를 수정하거나 우회하지 않는다.**

특히 다음과 같은 변경을 금지한다.

* 실패하는 테스트 삭제
* 테스트 skip 처리
* assertion 제거 또는 완화
* timeout을 과도하게 늘려 문제를 숨기는 변경
* 테스트 시나리오 자체 변경
* 특정 기능을 테스트하지 않도록 우회
* 서비스 변경 사항에 맞춰 테스트를 임의로 수정

실제 채점에서는 모든 팀에 동일한 공식 테스트가 사용되므로 로컬 테스트를 수정해서 통과해도 의미가 없다.

제공된 테스트 코드 자체의 문제라고 판단되는 경우 서비스 구현을 억지로 변경하지 말고 운영진에 확인한다.

---

# 3. 외부 동작 호환성 유지

## 내부 구현은 변경 가능하다

성능 개선을 위해 다음과 같은 내부 구현 변경은 허용한다.

* DB 쿼리 최적화
* 인덱스 추가
* Redis 캐싱
* 읽기 캐시
* 비동기 처리
* 데이터 반정규화
* 서버 구조 변경
* WebSocket 관련 구현 변경
* Socket.IO 내부 구조 변경
* 백엔드 Scale-out
* DB/Redis 분리
* 로드밸런싱
* Docker 구성 변경

단, 변경 이후에도 **E2E에서 관찰되는 사용자 동작은 동일해야 한다.**

---

## 외부 인터페이스를 임의로 변경하지 않는다

성능 개선 과정에서 다음 요소는 특히 주의한다.

* URL 및 라우팅
* API endpoint
* HTTP method
* request 형식
* response 형식
* HTTP status code
* 인증 방식
* 로그인/세션 동작
* E2E가 사용하는 DOM 요소
* E2E가 탐색하는 버튼/입력 요소
* 채팅방 생성 흐름
* 메시지 송수신 흐름
* 프로필 관련 흐름

리팩터링 전에 테스트 코드가 해당 요소에 의존하는지 먼저 확인한다.

---

# 4. 인증 및 세션 안정성을 우선한다

채팅 테스트는 인증 및 세션이 정상적으로 성립한 이후 진행된다.

인증이 실패하면 이후 채팅 시나리오가 연쇄적으로 실패할 수 있으므로 다음을 우선적으로 확인한다.

* 로그인 성공 여부
* 인증 정보 유지 여부
* 세션 또는 토큰 정상 전달
* Cookie 설정
* CORS 설정
* 프론트/백엔드 도메인 설정
* Scale-out 환경에서의 세션 공유 문제

특히 백엔드를 여러 대로 확장할 경우 특정 서버에만 인증 상태가 존재하는 구조가 되지 않도록 주의한다.

---

# 5. 채팅 기능 변경 규칙

Socket.IO 등 내부 통신 구현은 변경할 수 있다.

단, E2E 관점에서 다음 결과는 동일해야 한다.

* 채팅방 생성
* 채팅방 입장
* 메시지 전송
* 메시지 수신
* 메시지 화면 표시

Socket.IO를 다른 WebSocket 구현으로 변경하는 경우 클라이언트와 서버 프로토콜을 함께 변경해야 한다.

한쪽만 변경하여 통신 프로토콜이 어긋나는 상태를 만들지 않는다.

---

# 6. 데이터 일관성 관련 규칙

테스트에서 검증하지 않는 강한 최종 일관성을 성능을 희생하면서까지 구현할 필요는 없다.

예를 들어 테스트에서 다음 사항을 검증하지 않는다면 성능 최적화를 위해 일시적인 지연을 허용할 수 있다.

* 방 생성 직후 목록에 즉시 반영
* 비동기 집계 결과의 즉시 반영
* 캐시와 DB 사이의 짧은 시간 차이

따라서 필요하다면 다음 전략을 고려할 수 있다.

* 읽기 캐시
* eventual consistency
* 비동기 처리
* 배치 반영
* 집계 데이터 캐싱

단, 다음은 허용하지 않는다.

* 데이터 유실
* 사용자 동작 실패
* 잘못된 응답
* 테스트에서 확인하는 데이터의 불일치

---

# 7. AI 기능

AI 기능은 올해 필수 채점 대상이 아니다.

백엔드에 OpenAI 관련 코드가 남아 있더라도 `OPENAI_API_KEY` 설정 및 AI 기능 유지에 불필요한 시간을 사용하지 않는다.

AI 기능보다 다음 항목을 우선한다.

* E2E 안정성
* 채팅 성능
* DB 성능
* Redis 성능
* WebSocket 안정성
* 서버 자원 사용량
* Scale-out

사용하지 않는 AI 코드가 있더라도 다른 기능에 영향을 주지 않는다면 성능 개선 과정에서 우선적으로 수정할 필요가 없다.

---

# 8. 부하테스트 최적화 원칙

사전 테스트는 실제 부하테스트와 동일한 방식이지만 **100명 규모**로 진행된다.

100명 테스트를 통과했다고 실제 테스트에서도 안전하다고 가정하지 않는다.

사전 테스트에서는 다음 지표의 절대값뿐 아니라 증가 추세를 확인한다.

* 오류율
* 응답시간
* throughput
* CPU
* Memory
* Network
* DB connection
* Redis connection
* WebSocket connection
* GC
* 서버별 부하 편차

부하가 증가할수록 특정 지표가 급격하게 증가한다면 실제 테스트에서 병목이 될 가능성이 높다.

---

# 9. 성능 최적화 작업 절차

성능 문제를 발견하면 추측만으로 구조를 변경하지 않는다.

다음 순서를 따른다.

1. 현재 상태 측정
2. 병목 지점 식별
3. 가설 수립
4. 하나의 주요 변경 적용
5. 동일 조건으로 재측정
6. 결과 비교
7. E2E 회귀 테스트
8. 효과가 확인되면 다음 병목 분석

가능하면 여러 최적화를 동시에 적용하지 않는다.

여러 변경을 한 번에 적용하면 어떤 변경이 성능 개선 또는 장애를 발생시켰는지 판단하기 어렵다.

---

# 10. 인프라 변경 원칙

처음부터 복잡한 분산 구조를 만들기보다 단순한 구조에서 시작하여 실제 측정을 기반으로 확장한다.

기본적인 접근 순서는 다음과 같다.

```text
단일 구성
    ↓
부하테스트
    ↓
병목 확인
    ↓
병목 계층 분리
    ↓
Scale-out / Scale-up
    ↓
재측정
```

대표적인 병목 후보는 다음과 같다.

* Backend
* WebSocket / Socket.IO
* MongoDB
* Redis
* Network
* CPU
* Memory

병목이 확인되지 않은 계층을 무작정 Scale-out하지 않는다.

---

# 11. HTTPS

최종 배포 환경에서는 HTTPS가 필수다.

가능하면 다음 구조를 사용한다.

```text
Client
  ↓ HTTPS
ALB :443
  ↓
Frontend / Backend
```

ACM 인증서를 ALB HTTPS Listener에 연결하는 방식을 우선 고려한다.

HTTP 환경에서만 정상 동작하는 상태로 작업을 완료하지 않는다.

---

# 12. 프론트엔드 / 백엔드 주소

부하테스트의 진입점은 프론트엔드 주소이다.

백엔드는 별도 서브도메인으로 구성할 수 있다.

예:

```text
Frontend
chat.goorm-ktb-{team}.goorm.team

Backend
api.goorm-ktb-{team}.goorm.team
```

분리할 경우 반드시 다음 설정을 함께 확인한다.

* CORS
* Cookie Domain
* SameSite
* Secure
* WebSocket Origin
* API Base URL

특히 HTTPS 환경에서 Cookie 및 WebSocket 연결 문제가 발생하지 않는지 확인한다.

---

# 13. Docker / 배포 구성

제공된 `docker-compose`는 수정 가능하다.

단일 인스턴스 구성에서 시작한 뒤 필요하다면 서비스별로 분리한다.

예:

```text
Frontend
Backend
MongoDB
Redis
Monitoring
```

서비스를 다른 인스턴스로 이동할 경우 다음을 함께 변경한다.

* hostname
* connection URL
* environment variable
* security group
* port
* health check
* Docker network 의존성

---

# 14. Node Exporter

모든 채점 대상 인스턴스에서 node exporter가 정상 실행되어야 한다.

Custom AMI를 만드는 경우에도 node exporter를 제거하지 않는다.

새 인스턴스를 생성하거나 AMI를 변경했다면 다음을 확인한다.

* node exporter 프로세스 실행
* exporter port 접근 가능
* Prometheus Target 등록
* Prometheus Targets 상태 UP

node exporter가 누락되면 CPU, Memory, Network 등 주요 모니터링 지표가 수집되지 않을 수 있다.

---

# 15. 모니터링

인스턴스 최대 수에는 Prometheus/Grafana 등 모니터링 인스턴스도 포함된다.

따라서 모니터링 인프라를 필요 이상으로 분리하지 않는다.

가능하면 Prometheus와 Grafana를 하나의 모니터링 인스턴스에 배치하는 것을 고려한다.

Grafana에서 데이터가 보이지 않는 경우 다음 순서로 확인한다.

```text
Exporter
   ↓
Prometheus Targets
   ↓
Prometheus Query
   ↓
Grafana Data Source
   ↓
Grafana Panel Query
```

Grafana Panel부터 수정하기 전에 Prometheus가 실제 데이터를 수집하고 있는지 먼저 확인한다.

---

# 16. 추가 부하테스트 도구

공식 채점 기준은 제공된 테스트 코드다.

k6 등 별도 도구는 **개발 및 성능 분석 보조 용도**로만 사용한다.

추가 부하테스트를 작성할 경우 공식 시나리오에서 중요한 흐름을 우선 재현한다.

예:

* 로그인
* 채팅방 생성
* 채팅방 입장
* 메시지 송수신
* 프로필 조회/변경

공식 테스트를 k6 테스트로 대체하지 않는다.

---

# 17. 파일 및 이미지

테스트에서 사용하는 이미지 크기는 팀별로 동일하게 통제된다.

따라서 특정 팀만 큰 이미지가 들어오는 상황을 가정하여 과도한 최적화를 할 필요는 없다.

다만 이미지 처리 과정에서 다음 문제가 발생하지 않는지는 확인한다.

* 메모리 급증
* 요청 timeout
* 파일 업로드 실패
* connection 고갈

---

# 18. AWS 보안 규칙

AWS Access Key, Secret Access Key 등 credential을 Git 저장소에 커밋하지 않는다.

다음 정보는 코드에 직접 작성하지 않는다.

```text
AWS_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY
OPENAI_API_KEY
DB_PASSWORD
REDIS_PASSWORD
JWT_SECRET
기타 Credential
```

환경변수 또는 GitHub Secrets를 사용한다.

Credential이 GitHub에 노출되었다면 단순히 최신 commit에서 삭제하는 것으로 끝내지 않는다.

필요한 경우 다음 조치를 수행한다.

1. 노출된 Credential 비활성화
2. 새로운 Credential 발급
3. Git history에서 Credential 제거
4. 환경변수 또는 Secret으로 이동

---

# 19. CloudFront 생성 주의사항

CloudFront 구성 시 권한 문제를 피하기 위해 불필요한 Plan을 선택하지 않는다.

Plan 선택으로 WAF가 함께 활성화되면 현재 계정 권한에서 생성이 실패할 수 있다.

---

# 20. 변경 후 필수 검증

코드 또는 인프라를 변경한 뒤 최소한 다음을 확인한다.

```text
[ ] 애플리케이션 빌드 성공
[ ] Frontend 정상 접속
[ ] Backend 정상 응답
[ ] HTTPS 정상 동작
[ ] 로그인 정상 동작
[ ] 인증/세션 유지
[ ] 채팅방 생성 가능
[ ] 채팅방 입장 가능
[ ] 메시지 송수신 가능
[ ] 프로필 기능 정상
[ ] E2E 23개 통과
[ ] 부하테스트 오류율 확인
[ ] 응답시간 확인
[ ] CPU / Memory 확인
[ ] DB / Redis 상태 확인
[ ] Prometheus Target 정상
[ ] Node Exporter 정상
```

성능이 개선되었더라도 E2E가 깨졌다면 해당 변경을 완료된 최적화로 판단하지 않는다.

---

# 21. 에이전트 작업 규칙

에이전트가 코드를 수정할 때 다음 규칙을 따른다.

### 반드시 해야 하는 것

* 변경 전 관련 E2E 테스트를 확인한다.
* 기존 API 및 사용자 동작을 파악한 뒤 수정한다.
* 성능 변경 전후를 동일 조건으로 비교한다.
* 성능 최적화 후 E2E를 실행한다.
* 실패 발생 시 로그와 메트릭을 근거로 원인을 분석한다.
* 인프라 변경 시 기존 환경변수 및 네트워크 의존성을 확인한다.
* 큰 구조 변경보다 측정된 병목을 우선 해결한다.

### 하지 말아야 하는 것

* E2E 테스트를 통과시키기 위해 테스트 코드를 수정하지 않는다.
* 테스트를 skip하지 않는다.
* assertion을 제거하지 않는다.
* API 계약을 임의로 변경하지 않는다.
* 테스트에서 사용하는 라우팅이나 화면 흐름을 임의로 변경하지 않는다.
* 측정 없이 추측만으로 대규모 아키텍처 변경을 하지 않는다.
* 성능을 위해 데이터 유실이나 오류 응답을 허용하지 않는다.
* Credential을 코드 또는 Git 저장소에 추가하지 않는다.
* E2E 통과 여부를 확인하지 않고 최적화 작업을 완료했다고 판단하지 않는다.

---

# 22. 최종 판단 기준

모든 최적화는 다음 질문을 기준으로 판단한다.

> **"공식 E2E 동작을 그대로 유지하면서 더 많은 동시 사용자를 더 낮은 오류율과 안정적인 응답시간으로 처리할 수 있는가?"**

성능 수치가 좋아지더라도 E2E가 실패하면 잘못된 최적화다.

반대로 테스트에서 요구하지 않는 기능이나 강한 일관성을 제거하거나 완화하여 성능을 확보하는 것은 허용할 수 있다.

따라서 작업의 우선순위는 항상 다음과 같다.

```text
E2E 호환성
    ↓
정확성 / 안정성
    ↓
병목 측정
    ↓
성능 개선
    ↓
Scale-out / 아키텍처 개선
```
