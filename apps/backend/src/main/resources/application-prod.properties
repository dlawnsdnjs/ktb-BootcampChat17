# Multi-instance deployments use Redis for Socket.IO session storage and Pub/Sub.
# Set SOCKETIO_STORE_TYPE=memory only for an intentional single-node fallback.
socketio.store.type=${SOCKETIO_STORE_TYPE:redis}
chat.store.type=${CHAT_STORE_TYPE:redis}
