# Copy this file to .env and adjust values for your environment

ENCRYPTION_KEY=change_me_64_hex_chars____________________________________
ENCRYPTION_SALT=change_me_32_hex_chars________________
JWT_SECRET=change_me_secure_jwt_secret

MONGO_URI=mongodb://localhost:27017/bootcamp-chat
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_USERNAME=
REDIS_PASSWORD=
REDIS_SSL=false

# Optional override. The dev/prod profiles default to redis; use memory only as a fallback.
# SOCKETIO_STORE_TYPE=memory

# Local deployment: selects LocalStorage + LocalFileService.
FILE_STORAGE_TYPE=local
FILE_UPLOAD_DIR=./uploads

# For an S3 deployment, start from .env.s3.template instead.

PORT=5001
WS_PORT=5002

OPENAI_API_KEY=sk-...
